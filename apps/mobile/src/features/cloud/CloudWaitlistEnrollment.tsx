import { Text, View } from "react-native";

export function CloudWaitlistEnrollment(props: { readonly onSignIn: () => void }) {
  void props;
  return (
    <View>
      <Text>Cloud access is disabled in this fork.</Text>
    </View>
  );
}
